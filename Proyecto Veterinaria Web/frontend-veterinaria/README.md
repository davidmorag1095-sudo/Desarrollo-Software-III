# Frontend Clínica Veterinaria

Primero ejecutar la API desde la carpeta `crud-veterinaria`:

```bash
uvicorn app.main:app --reload
```

Luego abrir este frontend con Live Server o ejecutar:

```bash
python -m http.server 5500
```

Abrir `http://127.0.0.1:5500`.

